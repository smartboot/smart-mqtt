# ============================================
# smart-mqtt 技术债务账本
# ============================================

meta:
  version: "1.0"
  total_debt_items: 0
  high_risk_count: 0
  redemption_rate_target: 0.20

debt_items:
  DEBT-001:
    area: "消息持久化"
    module: "store"
    description: "当前实现不支持百万级队列回溯查询，需要重构索引结构"
    introduced_version: "v1.4.0"
    introduced_date: "2026-01-15"
    planned_fix_version: "v1.7.0"
    risk_level: "high"
    interest: "每延迟一个版本，重构成本增加 30%"
    affected_features:
      - "消息回溯"
      - "离线消息"
    mitigation: "限制单队列回溯消息数"

  DEBT-002:
    area: "认证授权"
    module: "security"
    description: "硬编码的权限判断逻辑散落在多处，需要抽象 SPI 接口"
    introduced_version: "v1.2.0"
    introduced_date: "2025-12-01"
    planned_fix_version: "v1.6.0"
    risk_level: "medium"
    interest: "新增认证方式开发周期 +2 天"
    affected_features:
      - "ACL 控制"
      - "多租户"
    mitigation: "新增统一封装工具类"

  DEBT-003:
    area: "测试覆盖"
    module: "integration-test"
    description: "集群场景自动化测试覆盖率不足 30%"
    introduced_version: "v1.0.0"
    introduced_date: "2025-10-01"
    planned_fix_version: "v1.6.0"
    risk_level: "medium"
    interest: "线上回归问题概率 × 2"
    affected_features:
      - "所有集群特性"
    mitigation: "增加手动测试 Checklist"

redemption_rules:
  mandatory_redemption_ratio: 0.20
  high_risk_must_fix_within: 2
  auto_alerts: true
  debt_cap_per_version: 3
